#include "vector.h"
#include<iostream>
#include<vector>
using std::vector;

int main() 
{
	

	Vector v1(3);
	v1.Reserve(6);
	return 0;
	
	
	
	//int a = 1, b = 5, c;
	//c = a * b; //R value, temp object or data in memory
	//int& c_ref = c;
	//int&& ab_ref = (a * b); //cannot refernce an rValue without double (&&)
	//Vector v1(3);
	
}
